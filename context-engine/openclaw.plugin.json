{
  "id": "context-engine",
  "name": "Context Engine",
  "description": "Topic-based dynamic context loading — classifies messages by topic, compresses inactive topics, loads only relevant context",
  "version": "0.1.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "enabled": {
        "type": "boolean",
        "description": "Enable/disable context engine"
      },
      "maxTopics": {
        "type": "number",
        "description": "Maximum number of topic summaries to keep"
      }
    }
  }
}
